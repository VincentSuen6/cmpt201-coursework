#include <stdio.h>
#include <stdlib.h>
#include <String.h>
#define

int main() {

  char *tokens
  char *text = NULL;
  size_t len = 0;

  while (nread != -1) {
    printf("Please ejter text!");
    nread = getline(s&text, &len, stdin);
    printf("tokens: \n");
    token = strtok_r(text, &saveptr);
    while (token) {
      printf("%s", token);
      token = strtok_r(NULL, &saveptr);
      if (token) {
        printf("/n")
      }
    }
  }
free(text);
return 0;
}
